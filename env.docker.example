# Переменные для Docker (скопируйте в .env в корне проекта).
# Для docker-compose: POSTGRES_PASSWORD и при необходимости логин/пароль приложения.

# Пароль пользователя PostgreSQL (по умолчанию postgres)
POSTGRES_PASSWORD=postgres

# Логин и пароль входа в приложение (по умолчанию user / password)
APP_SECURITY_USER_NAME=user
APP_SECURITY_USER_PASSWORD=password
