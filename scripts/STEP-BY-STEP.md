# Пошаговая инструкция развертывания

## ✅ Шаг 1: Настройка переменных окружения для БД

```bash
# Установите переменные (замените 'ваш_пароль' на реальный пароль PostgreSQL)
export SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/cursospring
export SPRING_DATASOURCE_USERNAME=postgres
export SPRING_DATASOURCE_PASSWORD=ваш_пароль
export APP_UPLOAD_DIR=/opt/cursospring/data/uploads

# Проверка
echo $SPRING_DATASOURCE_URL
```

---

## ✅ Шаг 2: Переход в каталог проекта

```bash
cd /opt/cursospring
# Или туда, куда вы скопировали/клонировали проект
```

---

## ✅ Шаг 3: Сборка и запуск бэкенда

### Вариант А: Автоматически (скрипт)

```bash
# Сделайте скрипты исполняемыми (если ещё не сделано)
chmod +x scripts/*.sh

# Сборка
./scripts/deploy-backend.sh

# Запуск
./scripts/start-backend.sh
```

### Вариант Б: Вручную

```bash
# Сборка проекта
mvn clean package -DskipTests

# Создание каталога для загрузок
mkdir -p data/uploads

# Запуск бэкенда
java -jar target/cursospring-0.0.1-SNAPSHOT.jar
```

**Бэкенд запущен на http://localhost:8080**

Проверка работы:
```bash
curl http://localhost:8080/api/auth/me
# Должен вернуть: {"authenticated":false}
```

---

## ✅ Шаг 4: Установка зависимостей фронтенда

```bash
cd /opt/cursospring/frontend
npm install
```

---

## ✅ Шаг 5: Запуск фронтенда

### Вариант А: Dev-сервер (для разработки/тестирования)

```bash
cd /opt/cursospring/frontend
npm run dev
```

**Фронтенд доступен на http://localhost:5173**

Vite автоматически проксирует `/api` запросы к `http://localhost:8080` (настроено в `vite.config.ts`).

### Вариант Б: Продакшен-сборка + веб-сервер

```bash
cd /opt/cursospring/frontend

# Сборка
npm run build

# Результат в frontend/dist/
```

Затем настройте nginx (см. `DEPLOY.md` раздел "Вариант А: Сборка статики + веб-сервер").

---

## ✅ Шаг 6: Проверка работы приложения

1. Откройте браузер: **http://localhost:5173** (dev) или ваш домен (prod)
2. Войдите: **user** / **password**
3. Проверьте:
   - Страница "Задачи" — создание, редактирование, удаление задач
   - Страница "Файлы" — загрузка, скачивание, удаление файлов

---

## 🔧 Управление бэкендом

### Остановка
```bash
/opt/cursospring/scripts/stop-backend.sh
```

### Просмотр логов
```bash
tail -f /opt/cursospring/logs/backend.log
```

### Перезапуск
```bash
/opt/cursospring/scripts/stop-backend.sh
/opt/cursospring/scripts/start-backend.sh
```

---

## 🚀 Запуск в фоне (screen/tmux)

Если хотите запустить бэкенд и фронтенд в фоне:

```bash
# Установка screen (если нет)
apt install -y screen

# Запуск screen сессии для бэкенда
screen -S backend
cd /opt/cursospring
java -jar target/cursospring-0.0.1-SNAPSHOT.jar
# Нажмите Ctrl+A, затем D для отсоединения

# Запуск screen сессии для фронтенда
screen -S frontend
cd /opt/cursospring/frontend
npm run dev
# Нажмите Ctrl+A, затем D для отсоединения

# Возврат к сессиям
screen -r backend  # или frontend
```

---

## 📝 Быстрая справка

| Действие | Команда |
|----------|---------|
| Сборка бэкенда | `mvn clean package -DskipTests` |
| Запуск бэкенда | `java -jar target/cursospring-0.0.1-SNAPSHOT.jar` |
| Остановка бэкенда | `./scripts/stop-backend.sh` |
| Логи бэкенда | `tail -f logs/backend.log` |
| Установка зависимостей фронта | `cd frontend && npm install` |
| Dev-сервер фронта | `cd frontend && npm run dev` |
| Сборка фронта | `cd frontend && npm run build` |

---

## ❓ Troubleshooting

### Бэкенд не запускается
```bash
# Проверьте переменные окружения
echo $SPRING_DATASOURCE_URL
echo $SPRING_DATASOURCE_USERNAME
echo $SPRING_DATASOURCE_PASSWORD

# Проверьте подключение к БД
psql -U postgres -d cursospring -c "SELECT 1;"

# Проверьте логи
tail -f logs/backend.log
```

### Фронтенд не подключается к бэкенду
```bash
# Проверьте, что бэкенд запущен
curl http://localhost:8080/api/auth/me

# Проверьте порты
netstat -tlnp | grep -E '8080|5173'
```

### Ошибки при сборке
```bash
# Очистка и пересборка бэкенда
mvn clean
mvn package -DskipTests

# Очистка и пересборка фронтенда
cd frontend
rm -rf node_modules dist
npm install
npm run build
```
